mutation createGame($playerLimit: Int!, $seed: ByteArray) {
    createGame(
        createGameInput: {
            playerLimit: $playerLimit,
            seed: $seed,
        }
    ) {
        ...gameProperties
    }
}